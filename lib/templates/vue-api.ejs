import httpRequest1 from '@/service'

interface IHomeDate {
  data: any
  returnCode: string
  success: boolean
}

httpRequest1
  .request<IHomeDate>({
    url: '/home/multidata',
    method: 'GET',
    interceptors: {
      requestSuccessFn: (config) => {
        console.log('/home/multidata混入的请求成功拦截')
        return config
      },
      responseSuccessFn(res) {
        console.log('/home/multidata混入的响应成功拦截')
        return res
      },
    },
  })
  .then((res) => {
    console.log(res)
  })
  .catch((err) => {
    console.log(err)
  })
